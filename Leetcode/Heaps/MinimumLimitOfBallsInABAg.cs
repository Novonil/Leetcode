using NetTopologySuite.Utilities;
using System;
using System.Collections.Generic;
using System.Text;

namespace Leetcode.Heaps
{
	class MinimumLimitOfBallsInABAg
	{
		//public int MinimumSize(int[] nums, int maxOperations)
		//{
		//	PriorityQueue<int> maxHeap = new PriorityQueue<int>(maxOperations, Comparer<int>.Create((a, b) => { return b.CompareTo(a); }));

		//	foreach(int num in nums)
		//	{
		//		maxHeap.Add(num);
		//		if(maxHeap.Size > maxOperations)
		//		{
		//			maxHeap.Poll();
		//		}
		//	}	

		//}
	}
}
